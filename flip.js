
const radius = 2.1627;
const rawArea = Math.PI * Math.pow(radius, 2);
const finalArea = Math.floor(rawArea);
console.log(`Raw Area: ${rawArea.toFixed(4)}`);
console.log(`Final (Floored) Area: ${finalArea}`);
